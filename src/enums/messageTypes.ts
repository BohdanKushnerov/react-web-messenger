export enum messageTypes {
  Text = 'text',
  AttachedFiles = 'attachedFiles',
  VoiceMessage = 'voiceMessage',
  // Image = 'image',
  // Video = 'video',
  // Audio = 'audio',
  // Location = 'location',
  // Contact = 'contact',
  // Sticker = 'sticker',
  // Link = 'link',
  // Event = 'event',
}
